# .env.example
# Voeg hier je Gemini API sleutel toe voor lokale ontwikkeling of externe hosting (zoals Netlify)
# In Netlify: Site configuration > Environment variables > Add variable
VITE_GEMINI_API_KEY=
